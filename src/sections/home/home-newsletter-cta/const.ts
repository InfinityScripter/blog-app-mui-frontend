// Newsletter copy (label / title / text / button / placeholder / note /
// success / error / invalidEmail) is resolved per-locale via
// `useTranslations("home")` under `home.newsletter.*` in `HomeNewsletterCta`
// and `NewsletterForm`. No static copy lives here.
export {};
