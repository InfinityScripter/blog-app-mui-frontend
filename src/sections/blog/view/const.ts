// ----------------------------------------------------------------------

// Publish-status filter tabs for the dashboard post list ("all" plus each
// publish state). Plain strings — the tab render compares against literal
// values and `filters.state.publish`.
export const POST_PUBLISH_TABS: string[] = ["all", "published", "draft"];
