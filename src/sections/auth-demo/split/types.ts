// ----------------------------------------------------------------------

export type SplitSignInFormProps = {
  isSubmitting: boolean;
};

export type SplitSignUpFormProps = {
  isSubmitting: boolean;
};

export type SplitUpdatePasswordFormProps = {
  isSubmitting: boolean;
};
